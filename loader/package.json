{
  "name": "@disputeshield/loader",
  "version": "0.1.0",
  "private": true,
  "description": "The ~4KB script that creates DisputeShield's sandboxed iframe",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/loader.js --bundle --minify --format=iife --outfile=dist/loader.js",
    "test": "node --test test/*.test.js",
    "size": "../scripts/check-loader-size.sh"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
